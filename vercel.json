{
  "version": 2,
  "buildCommand": "cd server && npm install",
  "outputDirectory": "server",
  "env": {
    "NODE_ENV": "production",
    "PORT": "5000"
  }
}
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/client/dist/$1"
    }
  ]
}
